<%= f.error_messages %>

<div class="control-group">
  <%= f.label :start_date, :class => 'control-label' %>
  <div class="controls">
    <%= f.datetime_select :start_date, :class => 'text-field' %>
  </div>
</div>
<div class="control-group">
  <%= f.label :end_date, :class => 'control-label' %>
  <div class="controls">
    <%= f.datetime_select :end_date, :class => 'text-field' %>
  </div>
</div>

<%= f.fields_for :documents do |builder| %>
  <%= render "document_fields", :f => builder %>
<% end %>
<p><%= f.link_to_add "Einen Bericht hinzufügen", :documents %></p>

<div class="form-actions">
  <%= f.submit "Speichern", :class => 'btn btn-primary' %>
  <% if @visit.new_record? %>
    <%= link_to 'Abbrechen', survey_path(@visit.survey), :class => 'btn' %>
  <% else %>
    <%= link_to 'Abbrechen', survey_visit_path(@visit.survey, @visit), :class => 'btn' %>
  <% end %>
</div>